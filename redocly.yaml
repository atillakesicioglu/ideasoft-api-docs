extends:
  - recommended

apis:
  admin-api@latest:
    root: openapi/domains/admin-api/products.json
  store-api@latest:
    root: openapi/domains/store-api/products.json

rules:
  no-unresolved-refs: warn
  operation-operationId: warn
  security-defined: off
  info-license: off
